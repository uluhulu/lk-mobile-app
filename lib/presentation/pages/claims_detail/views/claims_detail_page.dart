import 'package:flutter/material.dart';

class ClaimsDetailProvider extends StatelessWidget {
  const ClaimsDetailProvider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class ClaimsDetailContent extends StatelessWidget {
  const ClaimsDetailContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
